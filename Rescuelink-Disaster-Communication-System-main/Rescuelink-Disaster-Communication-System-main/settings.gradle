pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // SH-05: osmbonuspack (RadiusMarkerClusterer) is published on JitPack.
        maven { url 'https://jitpack.io' }
    }
}

rootProject.name = "RescueLink"
include ':app'
